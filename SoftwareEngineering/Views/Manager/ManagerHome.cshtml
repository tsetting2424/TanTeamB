@{
    ViewBag.Title = "Manager";
}

@section Header {
    <h1>@ViewBag.Title</h1>
}

@using (Html.BeginForm())
{

    
    <ul data-role="listview" data-inset="true" >
        <li data-role="list-divider">Manager Options</li>

        <li data-role="fieldcontain"> 
            @Html.ActionLink("Create User", "CreateAccount", "Manager")
        </li>

        <li data-role="fieldcontain">
            @Html.ActionLink("Update User", "UpdateAccounts", "Manager")
        </li>

        <li data-role="fieldcontain">   
            @Html.ActionLink("Reports", "reports", "Manager")
        </li>
     </ul> 
    
    <div id="ManagerContent">
    </div>
}

<script type="text/javascript">
    $(document).ready(function () {
        $.mobile.orientationChangeEnabled = true
       /* $('#CreateUserBtn').click(function () {
            var User = $('CreateUserBtn').val();
            var passingData = '{UserName:"' + User + '"}';
            alert(passingData);
            $.ajax({
                type: "POST",
                dataType: 'html',
                contentType: 'application/json',
                url: '@Url.Action("CreateAccount", "Manager")',
                data: passingData,
                cache: false,
                success: function (response) {
                    alert("worked " + response);
                    $("#ManagerContent").html(response);
                },
                error: function (request, status, error) {
                    alert("failed " + request.responseText);
                    alert(error);
                }
            });
        });*/
    });


</script>
